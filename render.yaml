services:
  # Flask Backend API
  - type: web
    name: fithub-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python test_import.py
    startCommand: chmod +x start.sh && ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SECRET_KEY
        generateValue: true
      - key: RAPIDAPI_KEY
        sync: false
      - key: FLASK_ENV
        value: production
    healthCheckPath: /
    autoDeploy: true

  # React Frontend
  - type: web
    name: fithub-frontend
    runtime: static
    buildCommand: cd client && npm install --legacy-peer-deps && npm run build
    staticPublishPath: client/build
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    autoDeploy: true
